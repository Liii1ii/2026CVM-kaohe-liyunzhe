# 微架构差异分析报告

---

## 一、报告元信息

- **生成时间**：2026-06-20 06:30:09
- **数据来源**：`process_perf.py` 输出的衍生指标数据（硬编码于 `analyze_perf.py` 的 DATA 字典中）
- **脚本版本**：analyze_perf.py v1.0
- **分析负载**：int64, matrixprod, read64, randset, queens

### 环境摘要

| 项目 | 值 |
|:---|:---|
| 虚拟化类型 | vmware |
| 频率锁定状态 | ⚠️ 未锁定 |
| AVX2 支持 | ✅ 支持 |
| AVX-512 支持 | ✅ 支持 |
| 可用核心数 | 2 |

### 原始衍生指标数据

| 负载 | IPC | L1 DCache Miss Rate | LLC Miss Rate | Branch Miss Rate | TLB Miss Rate |
|:---:|:---:|:---:|:---:|:---:|:---:|
| int64 | 0.8000 | 1.0% | N/A | 1.0% | 0.2% |
| matrixprod | 0.4000 | 2.5% | 0.5% | 0.5% | 0.1% |
| read64 | 0.3750 | 1.0% | 0.2% | 0.4% | <0.1% |
| randset | 0.3333 | 5.0% | 1.0% | 5.0% | 0.1% |
| queens | 1.2000 | 1.0% | 0.1% | 5.0% | 0.2% |

## 二、执行摘要

1. 本次测试覆盖 5 种典型负载，IPC 范围从 0.3333（randset）到 1.2000（queens）。
2. 检测到 4 项警告（P1），建议结合环境上下文解读。
3. 整体来看，该 vCPU 在 vmware 虚拟化环境下运行，频率未锁定，IPC 数据可能存在波动。

## 三、环境约束说明

⚠️ **频率未锁定**：CPU 调速器未锁定为 performance 模式，IPC 数据可能存在 ±5%~8% 的波动，横向对比需谨慎解读。

- NUMA 拓扑信息不可用（虚拟化环境常见），不影响单核性能测试。

- **虚拟化类型**：VMware

### 环境配置摘要

| 配置项 | 状态 |
|:---|:---|
| 虚拟化类型 | VMware |
| 频率锁定 | ⚠️ 未锁定 |
| AVX2 指令集 | ✅ 支持 |
| AVX-512 指令集 | ✅ 支持 |
| 可用 CPU 核心数 | 2 |
| NUMA 信息 | 不可用（虚拟化环境） |

## 四、数据有效性声明

### 逐指标可靠性评估

| 负载 | 指标 | 数值 | 可靠性 | 说明 |
|:---:|:---:|:---:|:---:|:---:|
| int64 | IPC | 0.8000 | 可靠 | — |
| int64 | L1 DCache Miss Rate | 1.0% | 可靠 | — |
| int64 | LLC Miss Rate | N/A | 不可用 | 该事件在虚拟化环境中不可用 |
| int64 | Branch Miss Rate | 1.0% | 可靠 | — |
| int64 | TLB Miss Rate | 0.2% | 可靠 | — |
| matrixprod | IPC | 0.4000 | 可靠 | — |
| matrixprod | L1 DCache Miss Rate | 2.5% | 可靠 | — |
| matrixprod | LLC Miss Rate | 0.5% | 可靠 | — |
| matrixprod | Branch Miss Rate | 0.5% | 可靠 | — |
| matrixprod | TLB Miss Rate | 0.1% | 可靠 | — |
| read64 | IPC | 0.3750 | 可靠 | — |
| read64 | L1 DCache Miss Rate | 1.0% | 可靠 | — |
| read64 | LLC Miss Rate | 0.2% | 可靠 | — |
| read64 | Branch Miss Rate | 0.4% | 可靠 | — |
| read64 | TLB Miss Rate | <0.1% | 可靠 | — |
| randset | IPC | 0.3333 | 可靠 | — |
| randset | L1 DCache Miss Rate | 5.0% | 可靠 | — |
| randset | LLC Miss Rate | 1.0% | 可靠 | — |
| randset | Branch Miss Rate | 5.0% | 可靠 | — |
| randset | TLB Miss Rate | 0.1% | 可靠 | — |
| queens | IPC | 1.2000 | 可靠 | — |
| queens | L1 DCache Miss Rate | 1.0% | 可靠 | — |
| queens | LLC Miss Rate | 0.1% | 可靠 | — |
| queens | Branch Miss Rate | 5.0% | 可靠 | — |
| queens | TLB Miss Rate | 0.2% | 可靠 | — |

## 五、关键异常发现

### 严重程度定义

| 等级 | 标识 | 含义 |
|:---:|:---:|:---|
| P0 | 🔴 严重 | 数据不可用或结果与理论严重背离，需要明确说明 |
| P1 | 🟡 警告 | 数据存在异常但可解释，需要在报告中重点说明 |
| P2 | 🟢 提示 | 数据正常，但存在值得关注的细节 |

### 异常列表

- **🟡 警告** [P1] [matrixprod] IPC 偏低（0.4000），可能存在访存延迟或执行单元争用。
  - 触发条件：`IPC 在 0.3~0.6 之间`
- **🟡 警告** [P1] [read64] IPC 偏低（0.3750），可能存在访存延迟或执行单元争用。
  - 触发条件：`IPC 在 0.3~0.6 之间`
- **🟡 警告** [P1] [randset] IPC 偏低（0.3333），可能存在访存延迟或执行单元争用。
  - 触发条件：`IPC 在 0.3~0.6 之间`
- **🟡 警告** [P1] 浮点矩阵乘法 IPC（0.4000）低于整数运算 IPC（0.8000），可能 SIMD 指令未生效。
  - 触发条件：`matrixprod 的 IPC 低于 int64`
- **🟢 提示** [P2] 部分事件在虚拟化环境中不可用（共 1 处 N/A），已在报告中标记。
  - 触发条件：`存在 N/A 数据`

## 六、各负载详细分析

### int64

**数值速览**：

| 指标 | 数值 |
|:---|:---:|
| IPC | 0.8000 |
| L1 DCache Miss Rate | 1.0% |
| LLC Miss Rate | N/A |
| Branch Miss Rate | 1.0% |
| TLB Miss Rate | 0.2% |

**三维归因分析**：

- **前端取指/解码**：分支预测失败率 1.0%，分支模式简单或编译器优化有效（可能使用 CMOV），前端取指效率高。
- **后端执行单元**：IPC 为 0.8000，流水线利用正常，存在轻度停顿，但未形成明显瓶颈。
- **访存子系统**：L1 DCache Miss Rate 为 1.0%，数据局部性极好，工作集完全在 L1 缓存内。

**综合结论**：

int64 属于计算密集型负载。

### matrixprod

**数值速览**：

| 指标 | 数值 |
|:---|:---:|
| IPC | 0.4000 |
| L1 DCache Miss Rate | 2.5% |
| LLC Miss Rate | 0.5% |
| Branch Miss Rate | 0.5% |
| TLB Miss Rate | 0.1% |

**三维归因分析**：

- **前端取指/解码**：分支预测失败率 0.5%，分支模式简单或编译器优化有效（可能使用 CMOV），前端取指效率高。
- **后端执行单元**：IPC 为 0.4000，流水线效率偏低，推测为访存延迟（Load-to-Use）或依赖冲突导致后端 Stall。 浮点矩阵乘法 IPC 偏低，虚拟化环境下 SIMD 指令集（AVX2/AVX-512）可能未被有效透传至虚拟机，建议在物理机复测验证。
- **访存子系统**：L1 DCache Miss Rate 为 2.5%，数据局部性良好，存在少量 L1 未命中，但影响有限。

**综合结论**：

matrixprod 属于计算密集型负载。

### read64

**数值速览**：

| 指标 | 数值 |
|:---|:---:|
| IPC | 0.3750 |
| L1 DCache Miss Rate | 1.0% |
| LLC Miss Rate | 0.2% |
| Branch Miss Rate | 0.4% |
| TLB Miss Rate | <0.1% |

**三维归因分析**：

- **前端取指/解码**：分支预测失败率 0.4%，分支模式简单或编译器优化有效（可能使用 CMOV），前端取指效率高。
- **后端执行单元**：IPC 为 0.3750，流水线效率偏低，推测为访存延迟（Load-to-Use）或依赖冲突导致后端 Stall。
- **访存子系统**：L1 DCache Miss Rate 为 1.0%，数据局部性极好，工作集完全在 L1 缓存内。

**综合结论**：

read64 属于访存密集型负载。

### randset

**数值速览**：

| 指标 | 数值 |
|:---|:---:|
| IPC | 0.3333 |
| L1 DCache Miss Rate | 5.0% |
| LLC Miss Rate | 1.0% |
| Branch Miss Rate | 5.0% |
| TLB Miss Rate | 0.1% |

**三维归因分析**：

- **前端取指/解码**：分支预测失败率 5.0%，存在大量不可预测分支，流水线冲刷频繁，前端取指存在瓶颈。
- **后端执行单元**：IPC 为 0.3333，流水线效率偏低，推测为访存延迟（Load-to-Use）或依赖冲突导致后端 Stall。
- **访存子系统**：L1 DCache Miss Rate 为 5.0%，数据局部性较差，频繁访问 L2/LLC，访存延迟可能成为主要瓶颈。

**综合结论**：

randset 属于访存密集型负载。

### queens

**数值速览**：

| 指标 | 数值 |
|:---|:---:|
| IPC | 1.2000 |
| L1 DCache Miss Rate | 1.0% |
| LLC Miss Rate | 0.1% |
| Branch Miss Rate | 5.0% |
| TLB Miss Rate | 0.2% |

**三维归因分析**：

- **前端取指/解码**：分支预测失败率 5.0%，存在大量不可预测分支，流水线冲刷频繁，前端取指存在瓶颈。
- **后端执行单元**：IPC 为 1.2000，流水线利用正常，存在轻度停顿，但未形成明显瓶颈。
- **访存子系统**：L1 DCache Miss Rate 为 1.0%，数据局部性极好，工作集完全在 L1 缓存内。

**综合结论**：

queens 属于分支密集型负载。

### 连续读 vs 随机访问对比

连续读模式（read64）的 LLC Miss Rate（0.2%）显著低于随机访问模式（randset，1.0%），表明硬件预取器有效工作，连续读模式下预取命中率高，符合预期。

## 七、综合结论与建议

### 1. 分类汇总

- **计算密集型**：int64、matrixprod
- **访存密集型**：read64、randset
- **分支密集型**：queens

### 2. 场景适用性评估

- 该 vCPU 在 **queens** 场景下 IPC 达到 1.2000，表现优异，适合整数计算密集型业务。
- 在 **randset** 场景下 IPC 仅为 0.3333，存在明显瓶颈，建议在物理机复测排除虚拟化干扰。
- 该 vCPU 在随机访问场景下表现受限（randset IPC=0.3333），不建议部署对内存延迟敏感的业务。

### 3. 后续建议

1. matrixprod IPC 偏低（0.4000），建议检查虚拟机 CPU 配置是否透传了 AVX2 指令集，并确认 `/proc/cpuinfo` 中 flags 包含 `avx2`。
2. 建议在锁定 CPU 频率（performance 模式）后重新测试，排除 Turbo Boost 和频率缩放对 IPC 的干扰，获得更准确的横向对比数据。

## 附录：Excel 条件格式热力图建议

在 Excel 中打开数据表格后，可使用以下条件格式规则快速生成热力图：

### IPC 着色规则（越高越绿）

| 条件 | 颜色 | 含义 |
|:---|:---|:---|
| IPC ≥ 1.5 | 🟢 绿色 | 流水线利用充分 |
| 0.8 ≤ IPC < 1.5 | 🟡 黄色 | 正常范围 |
| IPC < 0.8 | 🔴 红色 | 流水线效率偏低 |

### Miss Rate 着色规则（越低越绿）

| 条件 | 颜色 | 含义 |
|:---|:---|:---|
| Miss Rate < 2% | 🟢 绿色 | 缓存命中率高 |
| 2% ≤ Miss Rate < 5% | 🟡 黄色 | 正常范围 |
| Miss Rate ≥ 5% | 🔴 红色 | 缓存压力大，需关注 |

### Excel 操作步骤

1. 选中数据区域
2. 点击「开始」→「条件格式」→「新建规则」
3. 选择「使用公式确定要设置格式的单元格」
4. 输入对应公式（如 `=A1>=1.5`），设置填充色
5. 重复上述步骤完成所有阈值规则

---

*报告由 `analyze_perf.py` 自动生成于 2026-06-20 06:30:09*
